import java.util.ArrayList;

public class Staff extends LibraryUser{
    public Staff(String id, String passWord, String firstName, String lastName, String nationalId, int birthYear, String address) {
        super(id, passWord, firstName, lastName, nationalId, birthYear, address);
    }
}
